---
import { Image } from "astro:assets";
import Background from "../../../../public/Image/Shinrai/Background.webp";
import ShinraiLogo from "../../../../public/Image/Shinrai/ShinraiLogo.webp";
import MobileMenu from "../../GlobalComponents/MobileMenu";
import DarkMode from "../../GlobalComponents/DarkMode";
import Slider from "../components/ui/Slider";
---

<div class="relative w-full h-dvh">
  <Image
    src={Background}
    alt=""
    class="absolute inset-0 flex flex-col object-cover object-center w-full h-full object-fixed"
    width={1920}
    height={1080}
    sizes="100vw"
    widths={[640, 1024, 1600, 1920]}
  />

  <div
    class="absolute bottom-0 left-0 flex flex-col items-center justify-center w-full h-full bg-black/60"
  >
  </div>
  <div
    class="relative flex flex-col px-8 pt-8 items-center h-full justify-between w-full m-auto max-w-[100rem]"
  >
    <div
      class="grid items-center justify-center w-full h-auto grid-cols-2 gap-4 md:grid-cols-3"
    >
      <div class="flex justify-start md:justify-center">
        <Image
          src={ShinraiLogo}
          alt="ShinraiLogo"
          class="object-contain w-40 h-auto cursor-pointer md:w-60"
          width={800}
          height={800}
        />
      </div>

      <div class="flex justify-end order:2 md:order-3">
        <DarkMode bgColor="text-slate-100" client:load />
      </div>

      <div
        class="w-fit mx-auto col-span-2 md:col-span-1 order:3 md:order-2 border-slate-100 border-2 backdrop-blur-[4px] rounded-lg"
      >
        <MobileMenu
          textColor="text-slate-100 transition-all duration-900 ease-out"
          hoverColor="hover:text-slate-400"
          bgColor=""
          invertColor="text-white"
          client:load
        />
      </div>
    </div>

    <div
      class="flex flex-col items-center justify-center w-full h-auto max-w-[100rem] gap-1 md:gap-12 mx-auto text-start md:flex-1"
    >
      <div class="flex flex-col items-start justify-start gap-2">
        <h1 class="text-2xl font-bold text-center text-slate-100 md:text-4xl lg:text-6xl">
          Confía en tu fuerza, Domina tu mente
        </h1>

        <button
          class="p-2 text-sm text-center transition-all duration-700 ease-out bg-beige-100 text-zinc-950 hover:bg-beige-400"
        >
          Más información
        </button>
      </div>

      <Slider client:load />
    </div>
  </div>
</div>
